name: Evaluate Submission

on:
  workflow_dispatch:
    inputs:
      session_id:
        description: 'Candidate session ID'
        required: true
      challenge_id:
        description: 'Challenge number (1-6)'
        required: true
      candidate_code:
        description: 'Base64-encoded candidate Java code'
        required: true

jobs:
  evaluate:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - uses: actions/cache@v4
        with:
          path: ~/.m2
          key: maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            maven-

      - name: Inject candidate code
        run: |
          CHALLENGE_DIR="challenge-tests/challenge-${{ inputs.challenge_id }}"
          echo "${{ inputs.candidate_code }}" | base64 -d > \
            ${CHALLENGE_DIR}/src/main/java/com/assessment/CANDIDATE_SUBMISSION.java
          echo "Injected code into ${CHALLENGE_DIR}"
          cat ${CHALLENGE_DIR}/src/main/java/com/assessment/CANDIDATE_SUBMISSION.java | head -5

      - name: Run tests
        id: run_tests
        run: |
          cd challenge-tests/challenge-${{ inputs.challenge_id }}
          mvn test -q --no-transfer-progress 2>&1 | tee test_output.txt
          echo "exit_code=$?" >> $GITHUB_OUTPUT
        continue-on-error: true

      - name: Parse and report results
        run: |
          cd challenge-tests/challenge-${{ inputs.challenge_id }}

          TESTS_RUN=$(grep -oP 'Tests run: \K[0-9]+' test_output.txt | head -1 || echo "0")
          FAILURES=$(grep -oP 'Failures: \K[0-9]+' test_output.txt | head -1 || echo "0")
          ERRORS=$(grep -oP 'Errors: \K[0-9]+' test_output.txt | head -1 || echo "0")

          if [ -z "$TESTS_RUN" ] || [ "$TESTS_RUN" = "0" ]; then
            TESTS_RUN=0
            FAILURES=0
            ERRORS=0
          fi

          ACTUAL_PASSED=$(( TESTS_RUN - FAILURES - ERRORS ))
          if [ "$TESTS_RUN" -gt 0 ]; then
            SCORE=$(( ACTUAL_PASSED * 100 / TESTS_RUN ))
          else
            SCORE=0
          fi

          RAW_OUTPUT=$(cat test_output.txt | head -50 | base64 -w 0 || echo "")

          echo "Tests: $TESTS_RUN, Passed: $ACTUAL_PASSED, Score: $SCORE"

          curl -s -X POST "${{ secrets.BACKEND_CALLBACK_URL }}/api/webhook/github" \
            -H "Content-Type: application/json" \
            -H "X-Webhook-Secret: ${{ secrets.WEBHOOK_SECRET }}" \
            -d "{
              \"session_id\": \"${{ inputs.session_id }}\",
              \"challenge_id\": ${{ inputs.challenge_id }},
              \"deep_score\": ${SCORE},
              \"passed_count\": ${ACTUAL_PASSED},
              \"total_count\": ${TESTS_RUN},
              \"raw_output\": \"${RAW_OUTPUT}\"
            }" \
            && echo "Callback sent successfully" \
            || echo "Callback failed (backend may not be configured)"
